import React from 'react';

// Empty component per request to remove the Historical Signal Model Performance section
const StatsSection: React.FC = () => {
  // Return null instead of a section - this effectively removes the component from the DOM
  return null;
};

export default StatsSection;
